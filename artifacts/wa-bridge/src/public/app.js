const api=(u,o={})=>fetch(u,{headers:{'content-type':'application/json'},...o}).then(async r=>{const j=await r.json().catch(()=>({}));if(!r.ok)throw Error(j.error||r.statusText);return j});
const app=document.querySelector('#app'); let state={};
function auth(){app.innerHTML=`<main class="shell hero"><section><div class="brand">WA-Bridge</div><h1 class="title">Commercial WhatsApp Workspace Console</h1><p class="subtitle">Pair WhatsApp sessions, monitor live activity, manage validation buckets, and launch compliant group outreach from a premium web dashboard.</p></section><section class="card"><h2>Sign in</h2><p class="muted">Local credentials only. No email or OAuth required.</p><input id="u" placeholder="username"><br><br><input id="p" type="password" placeholder="password"><br><br><div class="row"><button id="login">Login</button><button id="register" class="secondary">Register</button></div><p id="err" class="muted"></p></section></main>`;['login','register'].forEach(id=>document.getElementById(id).onclick=async()=>{try{await api('/api/auth/'+id,{method:'POST',body:JSON.stringify({username:u.value,password:p.value})});dash()}catch(e){err.textContent=e.message}})}
async function dash(){try{state=await api('/api/dashboard')}catch{return auth()}app.innerHTML=`<main class="shell"><div class="top"><div><div class="brand">Control Center</div><h1>Workspace Dashboard</h1></div><button class="secondary" id="logout">Logout</button></div><section class="grid"><div class="card span4"><div class="muted">Sessions</div><div class="metric">${state.sessions.length}</div><span class="pill">${state.activeSockets.length} online</span></div><div class="card span4"><div class="muted">Active Links</div><div class="metric">${state.buckets.active}</div></div><div class="card span4"><div class="muted">Pending Validation</div><div class="metric">${state.buckets.main}</div></div><div class="card span6"><h2>Pair WhatsApp</h2><input id="label" placeholder="Workspace label"><br><br><input id="phone" placeholder="Phone with country code"><br><br><select id="method"><option value="qr">QR pairing</option><option value="code">Pairing code</option></select><br><br><button id="pair">Start pairing</button><div id="pairout" class="muted"></div></div><div class="card span6"><h2>Mass Outreach</h2><select id="sid">${state.sessions.map(s=>`<option value="${s.sessionId}">${s.label||s.phone} — ${s.status}</option>`).join('')}</select><br><br><select id="otype"><option value="allstatus">Group Status Broadcast</option><option value="allchat">Group Broadcast</option></select><br><br><textarea id="msg" placeholder="Professional message or URL"></textarea><br><br><div class="row"><button id="send">Launch</button><button id="stop" class="secondary">Stop</button></div></div><div class="card span6"><h2>Link Validator Buckets</h2><textarea id="links" placeholder="Paste invite links, one per line"></textarea><br><br><button id="addlinks">Add to main bucket</button></div><div class="card span6"><h2>.statusdesign Preview</h2><input id="url" placeholder="https://example.com"><br><br><input id="sdmsg" placeholder="Campaign message"><br><br><button id="preview">Render ASCII status</button><pre id="sdout" class="log"></pre></div><div class="card span12"><h2>Live Operations Log</h2><pre class="log">${(state.logs||[]).join('\n')}</pre></div></section></main>`;logout.onclick=()=>api('/api/auth/logout',{method:'POST'}).then(auth);pair.onclick=async()=>{let r=await api('/api/sessions',{method:'POST',body:JSON.stringify({label:label.value,phone:phone.value,method:method.value})});pairout.textContent='Pairing started. Waiting for code/QR...';setTimeout(async()=>{let p=await api('/api/sessions/'+r.sessionId+'/pairing');pairout.innerHTML=p.qr?`<img class="qr" src="${p.qr}">`:p.code?`Code: <b>${p.code}</b>`:p.error||'Still preparing...'},2500)};send.onclick=()=>api('/api/outreach',{method:'POST',body:JSON.stringify({sessionId:sid.value,type:otype.value,message:msg.value})}).then(dash).catch(e=>alert(e.message));stop.onclick=()=>api('/api/outreach/stop',{method:'POST',body:JSON.stringify({sessionId:sid.value})});addlinks.onclick=()=>api('/api/buckets/links',{method:'POST',body:JSON.stringify({links:links.value})}).then(dash);preview.onclick=()=>api('/api/statusdesign/preview',{method:'POST',body:JSON.stringify({url:url.value,message:sdmsg.value})}).then(r=>sdout.textContent=r.text);setTimeout(dash,5000)}
dash();
